import { test, expect } from '../../../utils/infra/persistent-context';
import { QuotePage } from '../../../pages/ga/components/quotePage';
import ENV from '../../../utils/infra/env';
import { loginToAEMAuthor } from '../../../utils/infra/auth-fixture';

const BASE = () => ENV.AEM_AUTHOR_URL || 'http://localhost:4502';

test.beforeEach(async ({ page }) => {
  await loginToAEMAuthor(page);
});

/*
 * Quote State Matrix
 *
 * 8 tests (4 valid + 2 responsive + 2 invalid).
 * Generated by state-matrix-generator.ts — viewport triplication collapsed.
 *
 * Selectors (from DOM probe):
 *   Section: .cmp-section--background-color-{bg}
 *   Disabled overlays excluded via :not([aria-disabled="true"])
 */

/** Section CSS class for a given background (from live AEM DOM) */
function sectionSel(bg: string) {
  return `.cmp-section--background-color-${bg}`;
}

// ── Valid: variant × theme × background at desktop (4 tests) ──

test.describe('Quote — State Matrix (Valid)', () => {
    test('[QUOTE-V] @matrix @regression default + dark-theme in white section', async ({ page }) => {
      const pom = new QuotePage(page);
      await pom.navigate(BASE());
      const container = page.locator(sectionSel('white')).first();
      await expect(container).toBeVisible();
      const el = container.locator('.cmp-quote:not([aria-disabled="true"])').first();
      await expect(el).toBeVisible();
    });

    test('[QUOTE-V] @matrix @regression default + dark-theme in slate section', async ({ page }) => {
      const pom = new QuotePage(page);
      await pom.navigate(BASE());
      const container = page.locator(sectionSel('slate')).first();
      await expect(container).toBeVisible();
      const el = container.locator('.cmp-quote:not([aria-disabled="true"])').first();
      await expect(el).toBeVisible();
    });

    test('[QUOTE-V] @matrix @regression default + auto-theme in white section', async ({ page }) => {
      const pom = new QuotePage(page);
      await pom.navigate(BASE());
      const container = page.locator(sectionSel('white')).first();
      await expect(container).toBeVisible();
      const el = container.locator('.cmp-quote:not([aria-disabled="true"])').first();
      await expect(el).toBeVisible();
    });

    test('[QUOTE-V] @matrix @regression default + auto-theme in slate section', async ({ page }) => {
      const pom = new QuotePage(page);
      await pom.navigate(BASE());
      const container = page.locator(sectionSel('slate')).first();
      await expect(container).toBeVisible();
      const el = container.locator('.cmp-quote:not([aria-disabled="true"])').first();
      await expect(el).toBeVisible();
    });
});

// ── Responsive: one spot-check per background at mobile (2 tests) ──

test.describe('Quote — State Matrix (Responsive)', () => {
    test('[QUOTE-R] @matrix @regression @mobile quote in white section @ mobile-390', async ({ page }) => {
      await page.setViewportSize({ width: 390, height: 844 });
      const pom = new QuotePage(page);
      await pom.navigate(BASE());
      const container = page.locator(sectionSel('white')).first();
      await expect(container).toBeVisible();
      const el = container.locator('.cmp-quote:not([aria-disabled="true"])').first();
      await expect(el).toBeVisible();
    });

    test('[QUOTE-R] @matrix @regression @mobile quote in slate section @ mobile-390', async ({ page }) => {
      await page.setViewportSize({ width: 390, height: 844 });
      const pom = new QuotePage(page);
      await pom.navigate(BASE());
      const container = page.locator(sectionSel('slate')).first();
      await expect(container).toBeVisible();
      const el = container.locator('.cmp-quote:not([aria-disabled="true"])').first();
      await expect(el).toBeVisible();
    });
});

// ── Invalid: contrast concerns (2 tests) ──

test.describe('Quote — State Matrix (Invalid Combos)', () => {
    test('[QUOTE-N] @matrix @negative default + light-theme on white (light-on-light)', async ({ page }) => {
      // light-theme on light background (white) has insufficient contrast
      // Auto-theme should be used instead. Verify container still renders.
      const pom = new QuotePage(page);
      await pom.navigate(BASE());
      const container = page.locator(sectionSel('white')).first();
      await expect(container).toBeVisible();
    });

    test('[QUOTE-N] @matrix @negative default + light-theme on slate (light-on-light)', async ({ page }) => {
      // light-theme on light background (slate) has insufficient contrast
      // Auto-theme should be used instead. Verify container still renders.
      const pom = new QuotePage(page);
      await pom.navigate(BASE());
      const container = page.locator(sectionSel('slate')).first();
      await expect(container).toBeVisible();
    });
});
