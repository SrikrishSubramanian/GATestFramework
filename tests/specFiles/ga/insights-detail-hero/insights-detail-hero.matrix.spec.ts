import { test, expect } from '../../../utils/infra/persistent-context';
import { InsightsDetailHeroPage } from '../../../pages/ga/components/insightsDetailHeroPage';
import ENV from '../../../utils/infra/env';
import { loginToAEMAuthor } from '../../../utils/infra/auth-fixture';

const BASE = () => ENV.AEM_AUTHOR_URL || 'http://localhost:4502';

test.beforeEach(async ({ page }) => {
  await loginToAEMAuthor(page);
});

/*
 * InsightsDetailHero State Matrix
 *
 * 4 tests (2 valid + 1 responsive + 1 invalid).
 * Generated by state-matrix-generator.ts — viewport triplication collapsed.
 *
 * Selectors (from DOM probe):
 *   Section: .cmp-section--background-color-{bg}
 *   Disabled overlays excluded via :not([aria-disabled="true"])
 */

/** Section CSS class for a given background (from live AEM DOM) */
function sectionSel(bg: string) {
  return `.cmp-section--background-color-${bg}`;
}

// ── Valid: variant × theme × background at desktop (2 tests) ──

test.describe('InsightsDetailHero — State Matrix (Valid)', () => {
    test('[INSIGHTS_DETAIL_HERO-V] @matrix @regression default + dark-theme in slate section', async ({ page }) => {
      const pom = new InsightsDetailHeroPage(page);
      await pom.navigate(BASE());
      const container = page.locator(sectionSel('slate')).first();
      await expect(container).toBeVisible();
      const el = container.locator('.cmp-insights-detail-hero:not([aria-disabled="true"])').first();
      await expect(el).toBeVisible();
    });

    test('[INSIGHTS_DETAIL_HERO-V] @matrix @regression default + auto-theme in slate section', async ({ page }) => {
      const pom = new InsightsDetailHeroPage(page);
      await pom.navigate(BASE());
      const container = page.locator(sectionSel('slate')).first();
      await expect(container).toBeVisible();
      const el = container.locator('.cmp-insights-detail-hero:not([aria-disabled="true"])').first();
      await expect(el).toBeVisible();
    });
});

// ── Responsive: one spot-check per background at mobile (1 tests) ──

test.describe('InsightsDetailHero — State Matrix (Responsive)', () => {
    test('[INSIGHTS_DETAIL_HERO-R] @matrix @regression @mobile insights-detail-hero in slate section @ mobile-390', async ({ page }) => {
      await page.setViewportSize({ width: 390, height: 844 });
      const pom = new InsightsDetailHeroPage(page);
      await pom.navigate(BASE());
      const container = page.locator(sectionSel('slate')).first();
      await expect(container).toBeVisible();
      const el = container.locator('.cmp-insights-detail-hero:not([aria-disabled="true"])').first();
      await expect(el).toBeVisible();
    });
});

// ── Invalid: contrast concerns (1 tests) ──

test.describe('InsightsDetailHero — State Matrix (Invalid Combos)', () => {
    test('[INSIGHTS_DETAIL_HERO-N] @matrix @negative default + light-theme on slate (light-on-light)', async ({ page }) => {
      // light-theme on light background (slate) has insufficient contrast
      // Auto-theme should be used instead. Verify container still renders.
      const pom = new InsightsDetailHeroPage(page);
      await pom.navigate(BASE());
      const container = page.locator(sectionSel('slate')).first();
      await expect(container).toBeVisible();
    });
});
